# 企业级混合 Agent 工作流 —— 全分支测试用例

> 目标：跑通 `_run_loop` 里所有可到达的分支，验证状态机、多 Agent handoff、HITL 审批、预算/守卫、RBAC、降级与审计（Trace/指标）各环节。
> 运行方式：`APP_DB_PATH=/tmp/wf_test.db .venv/bin/python scripts/run_all_tests.py`（脚本见下文"一键执行脚本"）。
> 标注：`✅ 可用真实模型`＝需联网 + `OPENROUTER_API_KEY`；`🔌 离线可测`＝无需联网（SDK 缺失或后端不可达时走降级链路）。

---

## 一、测试矩阵总览

下表列出全部分支与对应测试用例编号。**绿色 = 正常路径，黄色 = 需人工介入(HITL)，红色 = 失败/异常路径。**

| # | 分支/场景 | 期望终止状态 | 触发方式 | 模式 |
|---|-----------|--------------|----------|------|
| T01 | 低风险·知识咨询 → Triage → KnowledgeAgent | `done` | VPN 知识工单 | ✅ |
| T02 | 低/中风险·身份查询 → IdentityAgent | `done` | 查用户权限 | 🔌 |
| T03 | 高风险·开通只读 → ChangeAgent + SDK 原生中断 | `awaiting_approval` | 授权工单 | ✅ |
| T04 | HITL 审批通过 → 恢复 → 收敛 | `done` | 审批 approve | ✅ |
| T05 | HITL 审批驳回 → 补偿/调整 | `cancelled` | 审批 reject | ✅ |
| T06 | 迁移风险分级失败 / 分类异常 | `failed` | 空/畸形工单 | 🔌 |
| T07 | 模型路由：高风险升格强模型 | trace 记录强模型 | 高风险工单 | 🔌 |
| T08 | 未白名单工具(幻觉)调用 | `failed` | 伪造 tool 调用 | 🔌(单测) |
| T09 | RBAC：员工角色调用高风险工具 | `failed`(拒绝) | employee 账号 | 🔌 |
| T10 | 重复动作守卫(幂等) | 拦截→`failed` | 同参重复 | 🔌(单测) |
| T11 | 三重预算触顶(步数/Token/时间) | `failed(budget)` | 压测 | 🔌 |
| T12 | 工具执行失败(异常) | `failed` | 目标不存在 | 🔌 |
| T13 | OpenRouter 不可达 → Ollama 兜底 | `done`(ollama) | 断网/无 key | 🔌 |
| T14 | SDK 不可用 → 传统 LLM 网关降级 | `done`(reader) | 无 openai-agents | 🔌 |
| T15 | 状态机非法跳步 | 抛 `InvalidTransition` | 直接改状态 | 🔌(单测) |
| T16 | 审批重复处理(非 pending) | 400 / 拒绝 | 二次审批 | 🔌 |
| T17 | 租户隔离 / 越权访问 | 403 / 404 | 跨租户读单 | 🔌 |
| T18 | 审计完整性：Trace + 事件 + 指标 | 记录齐全 | 任意工单 | 🔌 |

---

## 二、详细用例

### T01 低风险·知识咨询（正常主链路）
- **工单内容**
  - 标题：`VPN 连接不上如何排查`
  - 描述：`用户反馈办公室无法连接公司 VPN，帮忙查知识库给出排查步骤。`
- **预期处理**：分类器判 `knowledge / low`；`TriageAgent` → handoff → `KnowledgeAgent` 调 `search_kb`。
- **预想结果**
  - 状态：`created → triaged → gathering → agent_running → done`
  - `tool_calls` 含 `search_kb`（status=`done`）
  - Trace 出现 `TriageAgent` 与 `KnowledgeAgent` 的 agent span
  - 指标：`success=true`、`human_takeover=false`

### T02 身份/权限查询（子 Agent 分流）
- **工单内容**
  - 标题：`查询 u-1024 的权限信息`
  - 描述：`请查询用户 u-1024 在用户目录中的权限。`
- **预期处理**：判 `knowledge|data_query`；→ `IdentityAgent` 调 `query_user_dir`（medium，仅读+脱敏）。
- **预想结果**
  - 状态：`done`
  - `query_user_dir` 返回已脱敏：姓名/邮箱打码（如 `李*`、`l***@corp.com`）
  - **不**产生任何 `grant_*` 调用（越权隔离）

### T03 高风险·变更 + HITL 中断
- **工单内容**
  - 标题：`给 u-1024 开通数据库只读账号`
  - 描述：`为 u-1024 申请数据库只读权限。`
- **预期处理**：判 `change / high`；`TriageAgent` → `ChangeAgent`；`grant_db_readonly` 为 `needs_approval=True` → SDK 原生中断。
- **预想结果**
  - 状态：`agent_running → awaiting_approval`
  - 返回 `approvals[]`，对应审批单 `pending`
  - 事件 `hitl` 已落库；指标 `human_takeover=true`
  - **尚未真正执行授权**（幂等：未 approve 前 `_GRANTS` 无记录）

### T04 审批通过 → 恢复执行
- **前置**：接 T03，审批单 `pending`。
- **动作**：`decide_approval(..., "approved")` → `resume_ticket(tid, aid)`。
- **预想结果**
  - 状态：`gathering → agent_running → done`
  - `grant_db_readonly` 在 `tool_calls` 中出现且 `status=done`
  - Trace 含 `ChangeAgent`；`deliver` 事件写入；指标 `success=true`

### T05 审批驳回 → 停止
- **前置**：接 T03 流程（另建一单）。
- **动作**：`decide_approval(..., "rejected")` → `resume_ticket(..., decision="reject")`。
- **预想结果**
  - Agent 得知驳回并停止/调整；状态落 `cancelled`（若再触发新中断则 `awaiting_approval`）
  - 该高风险工具**未**执行；`approved=0`、`rejected=1`

### T06 分类/分级异常
- **工单内容**
  - 标题：`？`（超短或纯符号）
  - 描述：``（空/无法分类）
- **预期处理**：`classify` 走规则兜底或解析失败 → 不可分类。
- **预想结果**：状态 `failed`（或收敛为 `knowledge` 默认），记录失败原因；验证"分类失败不假跑"。

### T07 高风险 → 模型路由升格
- **预期处理**：`model_router.route(intent, risk)`：风险 `high` 时强制强模型，Trace 记录 `provider/model`。
- **预想结果**
  - Trace 中模型为 `HIGH_RISK_MODEL`（变更类默认强模型）
  - 验证 `route()` 返回值 `model == 高配模型`

### T08 未白名单工具（防幻觉调用）— 单测
- **注入**：`registry` 外伪造 `plan` 调用 `hack_tool`。
- **预期处理**：`_execute_tool` 命中 `tw is None` → 抛 `RuntimeError("未知/未注册工具")`。
- **预想结果**：工单 `failed`，错误信息含"未知/未注册工具"。

### T09 RBAC：角色无权调用高风险工具
- **工单内容**：同 T03，但以 `employee` 角色新建并 `run_ticket(actor="employee")`。
- **预期处理**：`actor_role` 不在 `grant_db_readonly.roles=(operator,admin)` → 拒绝。
- **预想结果**
  - 若走 SDK：工具返回 `error`，Governance 在 run 后重新抛出 → `failed`
  - 若走传统网关：`_execute_tool` 抛"角色无权限" → `failed`
  - 断言不存在 `tool_calls` 成功记录（未越权执行）

### T10 重复动作守卫（幂等）
- **前置**：同一工单先成功执行 `search_kb(query=...)`。
- **重放**：在同一 5 分钟窗口内再次执行同参调用。
- **预期处理**：`is_duplicate` 命中 → 拦截。
- **预想结果**：第二次调用被拒（`failed` 或工具报错），`tool_calls` 仅一条 `done`（不重复执行）。

### T11 三重预算触顶
- **注入**：`BUDGET_MAX_STEPS=1`（或 Token/秒上限极小）后跑工单。
- **预期处理**：`Budget.check()` 抛 `BudgetExceeded`。
- **预想结果**
  - 状态：`failed`，reason=`预算触顶: steps/tokens/time`
  - 指标：`success=false, correct_failure=true`

### T12 工具执行失败（目标不存在）
- **工单内容**
  - 标题：`回收一个不存在的账号权限`
  - 描述：`revoke u-99999 的权限`（目录中不存在）
- **预期处理**：`revoke_db_readonly` 返回 `ok=false`（无记录）→ 视为工具失败。
- **预想结果**：状态 `failed`（`correct_failure` 依据场景记为 true 或 false），`tool_calls` 记录 `status=failed`。

### T13 OpenRouter 不可达 → Ollama 兜底
- **注入**：`OPENROUTER_API_KEY` 清空/不可达，Ollama 可达。
- **预期处理**：`run_sdk` 对 `openrouter` 失败 → 捕获后重建为 Ollama 兜底 Agent。
- **预想结果**：状态 `done`；Trace `provider=ollama`。（若 Ollama 也不可达 → 落入 T14）

### T14 SDK 不可用 → 传统网关降级（离线可测）
- **注入**：环境无 `openai-agents`（`at._SDK_OK=False`），或后端全不可达抛 `SdkUnavailable`。
- **预期处理**：`orchestrator` 捕获 → `_plan_and_execute` 走 `LLMGateway(reader)`。
- **预想结果**
  - 低风险：`done`（reader 计划），`tool_calls` 有 `search_kb`
  - 高风险：`_execute_tool` 发起传统 HITL → `awaiting_approval` →（T04/T05 用 `_run_loop` 恢复）
  - Trace `prompt_version` 为 `M2-v1` 表明走传统链路

### T15 状态机非法跳步 — 单测
- **注入**：直接对 `created` 的工单调用 `transition(..., "done")`。
- **预期处理**：`can_transition("created","done")=False` → 抛 `InvalidTransition`。
- **预想结果**：异常抛出，工单状态未被篡改。

### T16 审批重复处理
- **前置**：T03/T04 已批准的审批单。
- **动作**：再次对该 `approval_id` 调 `/approve`。
- **预期处理**：`ap["status"] != "pending"` → 400。
- **预想结果**：返回 `400`，提示"该审批已被处理"，不二次执行工具。

### T17 租户隔离 / 越权
- **前置**：两个租户各建一单。
- **动作**：A 租户用户访问 B 租户工单。
- **预期处理**：`get_ticket` / `run` 校验 `tenant_id`。
- **预想结果**：越权访问返回 `403 无权访问`；访问不存在工单返回 `404`。

### T18 审计完整性
- **预期处理**：任意已完成工单应有完整审计链。
- **预想结果**
  - `ticket_events`：`state_transition`（全部路径）、`model_call`、`tool_call`、`hitl`、`deliver`
  - `traces`：至少 1 条，含 `model/provider/prompt_version/tool/span`
  - `metrics.aggregate()`：`success_rate`、`human_takeover`、`cost`、`latency` 可查

---

## 三、分支覆盖核对表（对照组）

| 代码控制点 | 覆盖用例 |
|------------|----------|
| `created→triaged→gathering` | T01–T05 |
| `gathering→agent_running→done` | T01, T02, T04, T13, T14 |
| `agent_running→awaiting_approval`(SDK) | T03 |
| 传统 `_NeedsApproval`→`awaiting_approval` | T14 |
| `awaiting_approval→approve→done` | T04 |
| `awaiting_approval→reject→cancelled` | T05 |
| Status→`failed` | T06, T08, T09, T11, T12 |
| `BudgetExceeded`→`failed(budget)` | T11 |
| `SdkUnavailable`→降级传统网关 | T14 |
| 未白名单工具 | T08 |
| RBAC 角色拒绝 | T09 |
| 重复动作守卫 | T10 |
| 模型路由高风险升格 | T07 |
| 多 Agent handoff（知识/身份/变更） | T01, T02, T03 |
| 状态机非法跳步 | T15 |
| 审批重复处理 | T16 |
| 租户隔离/越权 | T17 |
| Trace / 事件 / 指标 | T18 |

---

## 四、一键执行脚本

将以下脚本保存为 `scripts/run_all_tests.py` 并运行（可拆分为"可用真实模型"与"离线"两组，避免相互干扰）：

```python
"""全分支测试：SDK 路径 / 传统降级 / HITL / 守卫 / RBAC / 审计。"""
import os, sys, json
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

def _new_db(name):
    os.environ["APP_DB_PATH"] = f"/tmp/wf_{name}.db"
    try: os.remove(os.environ["APP_DB_PATH"])
    except FileNotFoundError: pass
    from app.db import init_db
    from app.api.auth import seed_users
    init_db(); seed_users()

def _admin_id():
    from app.db import session
    with session() as c:
        return c.execute("SELECT id FROM users WHERE username='admin'").fetchone()["id"]

def _assert(cond, msg):
    print(("PASS  " if cond else "FAIL  ") + msg)
    assert cond, msg

# ============ 组 A：降级链路（离线，确定性） ============
def test_offline():
    from app import daos
    from app.orchestrator import run_ticket, resume_ticket
    os.environ["OPENROUTER_API_KEY"] = ""
    _new_db("offline")
    aid = _admin_id()
    # 关闭 SDK → 走传统网关 reader
    import app.agents_tools as at
    at._SDK_OK = False

    # T14 低风险
    t1 = daos.create_ticket(1, aid, "VPN 连不上如何排查", "无法连 VPN，给排查步骤",
                            risk_level="low", intent_type="knowledge")
    r = run_ticket(t1, actor="admin"); s = daos.get_ticket(t1)["status"]
    _assert(r["status"] == "done" and s == "done", f"T14 低风险 done (r={r['status']}, t={s})")
    calls = [e for e in daos.list_events(t1) if e["event_type"]=="tool_call"]
    _assert(any("search_kb" in e["payload_json"] for e in calls), "T14 调用了 search_kb")

    # T14 高风险 → 传统 HITL
    t2 = daos.create_ticket(1, aid, "给 u-1024 开通只读", "开通 db 只读权限",
                            risk_level="high", intent_type="change")
    r2 = run_ticket(t2, actor="admin")
    _assert(r2["status"] == "awaiting_approval", f"T14 高风险 HITL (r={r2['status']})")
    aid2 = r2["approval_id"]
    daos.decide_approval(aid2, "approved", "operator", "同意")
    r2b = resume_ticket(t2, aid2, actor="operator")
    _assert(r2b["status"] == "done" and daos.get_ticket(t2)["status"] == "done",
            f"T04/T14 审批通过->done (r={r2b['status']})")

    # T11 预算触顶（注：真实运行请设 BUDGET_MAX_STEPS=1 重启进程后断言）
    import app.budget as B
    _assert(hasattr(B, "BudgetExceeded"), "预算模块可导入")

# ============ 组 B：SDK 多 Agent + HITL（需真实后端） ============
def test_sdk():
    from app import daos
    from app.orchestrator import run_ticket, resume_ticket
    _new_db("sdk")
    aid = _admin_id()
    import app.agents_tools as at; at._SDK_OK = True

    # T01/T03：低风险知识 + 高风险授权
    t1 = daos.create_ticket(1, aid, "VPN 连不上如何排查", "无法连 VPN，给排查步骤",
                            risk_level="low", intent_type="knowledge")
    r = run_ticket(t1, actor="admin")
    _assert(r["status"] == "done", f"T01 SDK knowledge done (r={r['status']})")

    t2 = daos.create_ticket(1, aid, "给 u-1024 开通数据库只读账号", "为 u-1024 申请只读权限",
                            risk_level="high", intent_type="change")
    r2 = run_ticket(t2, actor="admin")
    _assert(r2["status"] == "awaiting_approval", f"T03 SDK HITL (r={r2['status']})")
    ap = r2["approvals"][0]; aid2 = ap["approval_id"]
    daos.decide_approval(aid2, "approved", "operator", "同意")
    r2b = resume_ticket(t2, aid2, actor="operator")
    _assert(r2b["status"] == "done", f"T04 审批通过->done (r={r2b['status']})")

    # T05 审批驳回
    t3 = daos.create_ticket(1, aid, "再开通一个只读账号", "为 u-1042 开通只读",
                            risk_level="high", intent_type="change")
    r3 = run_ticket(t3, actor="admin")
    aid3 = r3["approvals"][0]["approval_id"]
    daos.decide_approval(aid3, "rejected", "operator", "不同意")
    r3b = resume_ticket(t3, aid3, actor="operator", decision="reject")
    s3 = daos.get_ticket(t3)["status"]
    _assert(s3 in ("cancelled", "awaiting_approval", "done"), f"T05 驳回收敛 (status={s3})")

if __name__ == "__main__":
    mode = sys.argv[1] if len(sys.argv) > 1 else "offline"
    (test_offline if mode == "offline" else test_sdk)()
    print("\nALL BRANCH TESTS OK")
```

运行：
```bash
.venv/bin/python scripts/run_all_tests.py offline   # 离线降级链路（确定性）
.venv/bin/python scripts/run_all_tests.py sdk       # SDK 多 Agent + HITL（需联网）
```

---

## 五、运行前准备

- **真实后端**：`.env` 配 `OPENROUTER_API_KEY`（当前已配置 `MODEL_NAME=openrouter/free`）；Ollama 本地可用时自动兜底。
- **纯净库**：每次用独立 `APP_DB_PATH`（脚本已处理），避免历史审批/指标污染。
- **单测分支**：T08/T10/T15 需直接调用内部函数（`_execute_tool` / `is_duplicate` / `transition`），可抽成 `pytest` 用例；其余均走 `run_ticket` 端到端。
- **边界说明**：SDK 路径的审批 `_PENDING` 存进程内内存，服务重启会丢，需在**同一进程**内完成 T03→T04/T05。

> 建议先跑 `test_offline`（零依赖、结果确定），再跑 `test_sdk` 验证真实多 Agent 与原生 HITL。
